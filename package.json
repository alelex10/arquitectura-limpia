{
	"name": "arquitectura-limpia",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"build:domain": "pnpm --filter ./domain run build",
		"dev:backend": "pnpm run build:domain && pnpm --filter ./apps/backend run start:dev",
		"start:backend": " pnpm run build:domain && pnpm --filter ./apps/backend run start:prod"
	},
	"devDependencies": {
		"typescript": "^5.8.3",
		"nodemon": "^3.1.10"
	}
}
